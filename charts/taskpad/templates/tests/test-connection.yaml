apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "taskpad.fullname" . }}-test-connection"
  labels:
    {{- include "taskpad.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "taskpad.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
