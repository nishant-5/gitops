apiVersion: v1
kind: Service
metadata:
  name: {{ include "taskpad.fullname" . }}
  labels:
    {{- include "taskpad.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "taskpad.selectorLabels" . | nindent 4 }}
