apiVersion: v1
kind: Service

metadata:
  name: {{ .Release.Name }}-mongodb

spec:
  type: ClusterIP

  selector:
    app: {{ .Release.Name }}-mongodb

  ports:
    - port: {{ .Values.mongodb.service.port }}
      targetPort: 27017
      protocol: TCP